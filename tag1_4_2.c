{   unsigned int countUpCounter = 0;
    do {
    printf("%d\n" , countUpCounter); }
    while (countDownCounter = countUpCounter++ < 100);
    printf("Prepare for Landing!\n");
        
    return 0;
}
